<div><h1>Cari cafe</h1><p id="geo-copy">Boleh tau posisimu? Biar yang paling dekat muncul duluan.</p><button class="ngafe-button" type="button" data-ngafe-geo>Gunakan lokasi</button><input wire:model.live.debounce.250ms="q" placeholder="Cari cafe" aria-label="Cari cafe"><div>@foreach($areas as $key=>$label)<button wire:click="$set('area','{{ $key }}')" class="ngafe-chip">{{ $label }}</button>@endforeach</div><div>@foreach($categories as $category)<label><input type="checkbox" wire:model.live="categorySlugs" value="{{ $category->slug }}">{{ $category->name }}</label>@endforeach</div><p>{{ $results->count() }} cafe cocok</p><div class="ngafe-grid">@foreach($results as $cafe)<a class="ngafe-card" href="/{{ $cafe->city }}/{{ $cafe->slug }}"><h2>{{ $cafe->name }}</h2>@if(isset($cafe->distance_km))<p>{{ \App\Support\Format::distance($cafe->distance_km*1000) }}</p>@endif</a>@endforeach</div>@if($results->isEmpty())<p>Belum ketemu. @if($suggestion)Coba lepas '{{ $suggestion['slug'] }}' — ada {{ $suggestion['count'] }} cafe lain.@endif <a href="#">Usulkan cafe</a></p>@endif</div>

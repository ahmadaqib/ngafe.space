<x-layout.app :title="$cafe->name.' · ngafe.space'"><article><div class="ngafe-photo">@if($cafe->photos->first())<img class="ngafe-photo" src="{{ $cafe->photos->first()->url_full }}" alt="Foto {{ $cafe->name }}">@endif</div><h1>{{ $cafe->name }}</h1><p>{{ $cafe->rating_avg ? str_replace('.', ',', $cafe->rating_avg).' · '.$cafe->rating_count.' review' : 'Belum ada review — jadi yang pertama?' }} · {{ $opening->label }}</p>@if($opening->activeOverride)<p>{{ $opening->activeOverride }}</p>@endif<p>@foreach($cafe->categories as $category)<span class="ngafe-chip">{{ $category->name }}</span> @endforeach</p><a class="ngafe-button" href="https://www.google.com/maps/search/?api=1&query={{ $cafe->lat }},{{ $cafe->lng }}">Arah</a><h2>Review</h2>@forelse($cafe->reviews as $review)<section class="ngafe-card"><b>{{ $review->display_alias }}</b><p>{{ $review->body }}</p><small>{{ $review->created_at->diffForHumans() }}</small></section>@empty<p>Belum ada review.</p>@endforelse<a class="ngafe-button" href="{{ route('auth.google.redirect') }}">Pernah ke sini? Ceritakan versimu</a></article></x-layout.app>
